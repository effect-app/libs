---
"effect-app": minor
"@effect-app/infra": patch
"@effect-app/vue-components": patch
---

Remove `withDefaultConstructor` wrapper, use `S.withConstructorDefault` directly with `Effect.succeed`/`Effect.sync`.
